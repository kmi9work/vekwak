#posts
  -if @posts[0] != nil 
    = render :partial => "post", :collection => @posts
= will_paginate @posts
#post_button.yellow-block
  = link_to 'Написать ещё', new_post_path(:format => :js, :section_id => params[:section_id]), :remote => true
  